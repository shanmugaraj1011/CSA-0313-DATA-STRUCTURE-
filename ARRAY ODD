#include<stdio.h>
#include<conio.h>

int a[10],i,n;
printf("enter the number");
scanf("%d",&n);
printf("enter the elements one by one");
for(i=0;i<n;i++)
{
if(a[i]%2==0)
printf("%d",a[i]);
}
for(i=0;i<n;i++)
{
if(a[i]%2==1)
printf("%d",a[i]);
}
